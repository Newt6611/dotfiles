package main

func main() {
	// Poll Price Event example
	// opt, _ := redis.ParseURL("redis://localhost:6379/0")
	// client := redis.NewClient(opt)
	// sequencer := sequencer.New(client)
	// sequencer.PullPriceEvent(func(priceEvent models.PriceEvent) {
	// 	fmt.Print(priceEvent)
	// })
}

